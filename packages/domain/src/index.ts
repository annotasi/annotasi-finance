/**
 * Compile-time evidence that the domain package exists independently of any
 * framework, transport, persistence, or provider dependency.
 */
export const DOMAIN_FOUNDATION_STATUS = "framework-independent" as const;
